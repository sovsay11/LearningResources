using System;
using System.Collections.Generic;

namespace ShortcutDemo
{
  partial class Program
  {
    static void Main(string[] args)
    {
      // settings, text editor, c#, add missing user directives on paste
      // basically, I can copy this stuff now and paste it in another file, and the using directives will be imported right away
      List<string> names = new List<string>();

      Console.WriteLine("Hello World!");
    }
  }
}
