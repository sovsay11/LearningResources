using System.Collections.Generic;

namespace ShortcutDemo
{
  // smart line break (shift + enter), completes class and method declarations
  public class Demo
  {
    public void Test()
    {
      // settings, text editor, c#, show inheritance margins
      // look on the side of the classes now, they have a blue button that'll let you see the
      // inheritance of a class
      List<string> names = new List<string>();
      if (1 > 0)
      {

      }
    }
  }
}
